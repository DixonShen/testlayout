<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.dixonshen.testlayout.MainActivity">

    <ScrollView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scrollbars="vertical"
        android:fadingEdge="vertical"
        android:id="@+id/scrollView1">

    <LinearLayout style="@style/wrap_layout"
        android:id="@+id/linearLayout1"
        android:visibility="gone">
    <LinearLayout style="@style/my_linearLayout">
        <TextView style="@style/attr_text"
            android:text="CPU Version"/>

        <TextView style="@style/val_text"
            android:text="1.0.0"/>
    </LinearLayout>
    <View style="@style/split_line" />
    <LinearLayout style="@style/my_linearLayout">
        <TextView style="@style/attr_text"
            android:text="RAM"/>

        <TextView style="@style/val_text"
            android:text="2G"/>
    </LinearLayout>
    <View style="@style/split_line" />
    <LinearLayout style="@style/my_linearLayout">
        <TextView style="@style/attr_text"
            android:text="STATUS"/>

        <ImageView style="@style/img_arrow"
            android:id="@+id/arrow"/>
    </LinearLayout>

    </LinearLayout>
    </ScrollView>

</LinearLayout>
